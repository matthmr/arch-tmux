--- a/cmd-new-session.c	2023-11-02 14:09:12.361858718 -0300
+++ b/cmd-new-session.c	2023-11-02 14:08:23.694228080 -0300
@@ -349,8 +349,10 @@
 		free(cp);
 	}
 
-	if (!detached)
+	if (!detached) {
 		c->flags |= CLIENT_ATTACHED;
+		notify_client("client-attached", c);
+	}
 	if (!args_has(args, 'd'))
 		cmd_find_from_session(current, s, 0);

